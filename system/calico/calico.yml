apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: calico
  namespace: argo-cd
spec:
  project: system
  source:
    repoURL: "https://github.com/a19simma/argocd-cluster-bootstrap"
    path: system/calico/Chart
    targetRevision: HEAD
  destination:
    server: "https://kubernetes.default.svc"
    namespace: ""
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
