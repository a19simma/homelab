apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-pvc-clone
  namespace: harbor
spec:
  storageClassName: ceph-block
  dataSource:
    name: harbor-chartmuseum
    kind: PersistentVolumeClaim
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi